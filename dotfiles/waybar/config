{
	"layer": "top",
	"position": "top",

	"modules-left": ["hyprland/workspaces"],
	"modules-center": ["clock"],
	"modules-right": ["keyboard-state"],

	"clock": {
		"format": "{:%H:%M %d/%m}"
	},

	"keyboard-state": {
		"format": "{%layout}"
	}
}